import type { TokenColor } from './types'

// SCSS Token Colors Configuration
const scssTokenColors: TokenColor[] = [
  {
    // SCSS variables ($primary-color, $font-size, etc.)
    name: 'SCSS variables',
    scope: ['variable.other.scss'],
    settings: { foreground: '#f5c2e7' }
  }
]

export default scssTokenColors